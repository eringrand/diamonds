---
format:
  typst:
    toc: true
    section-numbering: 1.1.a
    columns: 2
---


```{r}
library(ggplot2)
library(lubridate)
```

```{r}
format(today(), "%b %e, %Y")
```

```{r}
diamonds |> 
  ggplot() +
  geom_bar(aes(cut)) 
```
